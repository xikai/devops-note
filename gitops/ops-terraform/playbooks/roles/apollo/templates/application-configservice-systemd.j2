[Unit]
Description={{ apollo_configservice }}
After=network.target


[Service]
Type=simple
Environment="JAVA_HOME=/usr/local/jdk"
ExecStart={{ apollo_home }}/{{ apollo_configservice }}/scripts/startup.sh
ExecStop={{ apollo_home }}/{{ apollo_configservice }}/scripts/shutdown.sh
Restart=on-abort

[Install]
WantedBy=multi-user.target
